import { HStack, Heading } from "@chakra-ui/react";

const Genres = () => {
  return (
    <HStack hideBelow="md">
      <Heading fontSize={26}>Genres</Heading>
    </HStack>
  );
};

export default Genres;
